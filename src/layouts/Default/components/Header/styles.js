import styled from "styled-components";

export const Container = styled.header`
  grid-area: header;

  display: flex;
  align-items: center;
  flex-direction: column;

  z-index: 13;

  padding: 18px;
  overflow: hidden;

  background-color: #3aae;
  box-shadow: 0px 2px 4px rgba(0, 0, 0, 0.25);
`;
